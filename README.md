## Helm Starter 
Compatible with helm2 only.
#### Requirements:
* ansible
* ansible-playbook
* helm
* kubernetes cluster
#### Run
```bash
ansible-playbook helm.yaml
```
OR
```bash
./start-helm.sh
```

